# Loop 节点实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在编排系统中新增「循环节点」，支持内嵌子任务或引用已有资源，由列表变量驱动循环执行。

**Architecture:** 在后端 `OrchestrationNode` 类型中加入 `loop` 配置字段，新增 `executeLoopNode` 函数处理四种子任务模式（inline/reference+sqlFile/shellFile/orchestration）。前端新增 `LoopNode` 渲染组件和 `LoopNodeConfig` 配置表单，复用现有 Config 组件。

**Tech Stack:** TypeScript, React, @xyflow/react, Node.js

---

## File Structure

| Action | File | Responsibility |
|--------|------|----------------|
| Modify | `backend/src/modules/orchestration/orchestration.types.ts` | 新增 LoopConfig 类型，type 联合加 'loop' |
| Modify | `backend/src/modules/orchestration/orchestration.service.ts` | 新增 executeLoopNode，executeNode 加 loop 分支 |
| Modify | `frontend/src/api/orchestration.ts` | 前端类型同步后端变更 |
| Create | `frontend/src/components/orchestration/nodes/LoopNode.tsx` | 循环节点渲染组件 |
| Create | `frontend/src/components/orchestration/panels/LoopNodeConfig.tsx` | 循环节点配置表单 |
| Modify | `frontend/src/components/orchestration/AddNodeDialog.tsx` | 加 loop 选项 |
| Modify | `frontend/src/components/orchestration/FlowEditor.tsx` | nodeTypes 加 loop |
| Modify | `frontend/src/components/orchestration/nodes/BaseNode.tsx` | TYPE_CONFIG 加 loop |
| Modify | `frontend/src/components/orchestration/panels/NodeConfigDrawer.tsx` | 加 loop 路由 |
| Modify | `frontend/src/components/orchestration/hooks/useOrchestrationEditor.ts` | OrchNodeType 加 'loop'，addNode 加默认值 |

---

### Task 1: 后端类型定义

**Files:**
- Modify: `backend/src/modules/orchestration/orchestration.types.ts`

- [ ] **Step 1: 添加 LoopConfig 相关类型并更新现有类型**

在 `OrchestrationNode` 的 `type` 联合中加入 `'loop'`，新增 `LoopSubTaskInline`、`LoopSubTaskReference`、`LoopConfig` 类型，并在 `OrchestrationNode` 上新增 `loop` 可选字段。在 `LogEntry` 的 `nodeType` 中加入 `'loop'`。

完整修改后的文件：

```typescript
export type ThresholdOperator = 'eq' | 'ne' | 'gt' | 'gte' | 'lt' | 'lte'

export type ColumnMapping = { source: string; target: string; isPartition?: boolean }

export type LoopSubTaskInline = {
  mode: 'inline'
  type: 'sql' | 'debug' | 'load' | 'wait' | 'shell'
}

export type LoopSubTaskReference = {
  mode: 'reference'
  refType: 'orchestration' | 'sqlFile' | 'shellFile'
  refId: string
  variableName?: string
}

export type LoopConfig = {
  variableId: string
  failureStrategy: 'stop' | 'continue'
  subTask: LoopSubTaskInline | LoopSubTaskReference
}

export type OrchestrationNode = {
  id: string
  name: string
  type: 'sql' | 'debug' | 'load' | 'wait' | 'shell' | 'loop'
  datasourceId: string
  sql: string
  sqlFileId?: string
  enabled: boolean
  thresholdOperator?: ThresholdOperator
  thresholdValue?: string
  targetDatasourceId?: string
  targetTable?: string
  mode?: 'insert' | 'replace' | 'truncate_insert'
  columnMappings?: ColumnMapping[]
  partitionColumns?: string
  loopVariableId?: string
  loop?: LoopConfig
  waitIntervalSec?: number
  waitTimeoutSec?: number
  waitStableCount?: number
  serverId?: string
  shellContent?: string
  shellFileId?: string
  position?: { x: number; y: number }
}

export type OrchestrationEdge = {
  id: string
  source: string
  target: string
}

export type Orchestration = {
  id: string
  name: string
  description: string
  nodes: OrchestrationNode[]
  edges: OrchestrationEdge[]
  createdAt: number
  updatedAt: number
}

export type OrchestrationInput = {
  name?: unknown
  description?: unknown
  nodes?: unknown
  edges?: unknown
}

export type LogEntry = {
  nodeId: string
  nodeName: string
  nodeType: 'sql' | 'debug' | 'load' | 'wait' | 'shell' | 'loop'
  status: 'success' | 'failed' | 'warning' | 'skipped' | 'running'
  sql: string
  affectedRows?: number
  result?: { columns: string[]; rows: Record<string, unknown>[] }
  thresholdPassed?: boolean
  actualValue?: string
  expectedValue?: string
  error?: string
  timestamp: number
  duration: number
}
```

- [ ] **Step 2: 提交**

```bash
git add backend/src/modules/orchestration/orchestration.types.ts
git commit -m "feat(loop-node): add LoopConfig types and extend OrchestrationNode"
```

---

### Task 2: 后端执行引擎 — executeLoopNode

**Files:**
- Modify: `backend/src/modules/orchestration/orchestration.service.ts`

- [ ] **Step 1: 在 executeNode 中加入 loop 分支**

在 `executeNode` 函数（约第 527 行）的 `if (node.type === 'wait')` 之前，加入：

```typescript
  if (node.type === 'loop') return executeLoopNode(node, pushLog || (() => {}), signal)
```

- [ ] **Step 2: 新增 executeLoopNode 函数**

在 `executeNode` 函数之前（`executeWaitNode` 之前即可），添加以下函数：

```typescript
async function executeLoopNode(node: OrchestrationNode, pushLog: (log: LogEntry) => void, signal?: AbortSignal): Promise<LogEntry> {
  const nodeStart = Date.now()
  try {
    if (!node.loop) throw new Error('未配置循环参数')
    const { variableId, failureStrategy, subTask } = node.loop

    // Resolve loop values
    const vars = await listVariables()
    const loopVar = vars.find(v => v.id === variableId || v.name === variableId)
    if (!loopVar) throw new Error(`循环变量不存在: ${variableId}`)
    const varName = loopVar.name
    const values = await resolveSqlVariableList(loopVar)

    if (values.length === 0) {
      return {
        nodeId: node.id, nodeName: node.name, nodeType: 'loop', status: 'success',
        sql: '', affectedRows: 0, actualValue: '0',
        expectedValue: '循环变量查询结果为空',
        timestamp: Date.now(), duration: Date.now() - nodeStart,
      }
    }

    let totalAffected = 0
    let failedCount = 0
    let lastError = ''

    for (let i = 0; i < values.length; i++) {
      if (signal?.aborted) throw new Error('已停止')
      const value = values[i].replace(VAR_INJECTION_RE, '')

      const iterLogId = `${node.id}-loop-${i}`
      const updateIterLog = (status: LogEntry['status'], msg: string, extra?: Partial<LogEntry>) => {
        pushLog({
          nodeId: iterLogId, nodeName: `${node.name} (循环 ${i + 1}/${values.length})`, nodeType: 'loop',
          status, sql: '', affectedRows: 0,
          actualValue: '', expectedValue: msg,
          timestamp: Date.now(), duration: Date.now() - nodeStart,
          ...extra,
        })
      }

      let iterLog: LogEntry
      try {
        if (subTask.mode === 'inline') {
          const tempNode: OrchestrationNode = {
            ...node,
            id: `${node.id}-inline`,
            type: subTask.type,
            name: node.name,
          }
          iterLog = await executeNode(tempNode, signal, pushLog, { varName, value })
        } else if (subTask.refType === 'sqlFile') {
          const file = await getSqlFile(subTask.refId)
          let sql = file.content.replaceAll(`\${${varName}}`, value)
          sql = await resolveVariables(sql)
          const ds = await getDataSource(node.datasourceId || file.datasourceId)
          const dsConfig = { type: ds.type, host: ds.host, port: ds.port, user: ds.user, password: ds.password, database: ds.database, filePath: ds.filePath }
          const result = await Promise.race([
            executeDbQuery(dsConfig, sql, 1),
            new Promise<never>((_, reject) => setTimeout(() => reject(new Error('查询超时 (30s)')), QUERY_TIMEOUT_MS)),
          ])
          iterLog = {
            nodeId: iterLogId, nodeName: `${node.name} (循环 ${i + 1}/${values.length})`, nodeType: 'loop',
            status: 'success', sql, affectedRows: result.rowCount,
            result: { columns: result.columns, rows: result.rows.slice(0, 5) },
            timestamp: Date.now(), duration: Date.now() - nodeStart,
          }
        } else if (subTask.refType === 'shellFile') {
          const file = await getShellFile(subTask.refId)
          let script = file.content.replaceAll(`\${${varName}}`, value)
          const result = node.serverId
            ? await executeShellOnServer(await getServer(node.serverId), script)
            : await executeLocal(script)
          iterLog = {
            nodeId: iterLogId, nodeName: `${node.name} (循环 ${i + 1}/${values.length})`, nodeType: 'loop',
            status: result.exitCode === 0 ? 'success' : 'failed', sql: script.slice(0, 500),
            error: result.exitCode !== 0 ? result.stderr.slice(0, 500) : undefined,
            result: { columns: ['stdout', 'stderr', 'exitCode'], rows: [{ stdout: result.stdout.slice(0, 500), stderr: result.stderr.slice(0, 200), exitCode: result.exitCode }] },
            timestamp: Date.now(), duration: result.duration,
          }
        } else if (subTask.refType === 'orchestration') {
          const subOrch = await readOrchFile(subTask.refId)
          const passVarName = subTask.variableName || varName
          const subLogs: LogEntry[] = []
          const subPushLog = (log: LogEntry) => { subLogs.push(log) }
          // Execute sub-orchestration's enabled nodes sequentially
          const subEnabled = subOrch.nodes.filter(n => n.enabled)
          for (const subNode of subEnabled) {
            if (signal?.aborted) throw new Error('已停止')
            const injectedNode: OrchestrationNode = {
              ...subNode,
              sql: subNode.sql?.replaceAll(`\${${passVarName}}`, value) ?? subNode.sql,
              shellContent: subNode.shellContent?.replaceAll(`\${${passVarName}}`, value) ?? subNode.shellContent,
            }
            const log = await executeNode(injectedNode, signal, subPushLog, { varName: passVarName, value })
            subPushLog(log)
            if (log.status === 'failed') break
          }
          const subFailed = subLogs.some(l => l.status === 'failed')
          const subAffected = subLogs.reduce((s, l) => s + (l.affectedRows ?? 0), 0)
          iterLog = {
            nodeId: iterLogId, nodeName: `${node.name} (循环 ${i + 1}/${values.length})`, nodeType: 'loop',
            status: subFailed ? 'failed' : 'success', sql: `子编排: ${subOrch.name}`,
            affectedRows: subAffected,
            error: subFailed ? '子编排执行失败' : undefined,
            timestamp: Date.now(), duration: Date.now() - nodeStart,
          }
          // Merge sub-orchestration logs into parent with prefixed nodeIds
          for (const sl of subLogs) {
            pushLog({ ...sl, nodeId: `${iterLogId}-${sl.nodeId}`, nodeName: `  └ ${sl.nodeName}` })
          }
        } else {
          throw new Error('未知的子任务模式')
        }
      } catch (err) {
        iterLog = {
          nodeId: iterLogId, nodeName: `${node.name} (循环 ${i + 1}/${values.length})`, nodeType: 'loop',
          status: 'failed', sql: '', error: err instanceof Error ? err.message : String(err),
          timestamp: Date.now(), duration: Date.now() - nodeStart,
        }
      }

      pushLog(iterLog)
      totalAffected += iterLog.affectedRows ?? 0
      if (iterLog.status === 'failed') {
        failedCount++
        lastError = iterLog.error ?? ''
        if (failureStrategy === 'stop') break
      }
    }

    const status = failedCount === values.length ? 'failed'
      : failedCount > 0 ? 'warning' : 'success'
    const msg = failedCount === 0
      ? `${values.length} 次循环全部成功，共 ${totalAffected} 行`
      : `${values.length} 次循环中 ${failedCount} 次失败，共 ${totalAffected} 行${lastError ? ` (最后错误: ${lastError})` : ''}`

    return {
      nodeId: node.id, nodeName: node.name, nodeType: 'loop', status,
      sql: '', affectedRows: totalAffected,
      actualValue: String(totalAffected), expectedValue: msg,
      timestamp: Date.now(), duration: Date.now() - nodeStart,
    }
  } catch (err) {
    return {
      nodeId: node.id, nodeName: node.name, nodeType: 'loop', status: 'failed',
      sql: '', error: err instanceof Error ? err.message : String(err),
      timestamp: Date.now(), duration: Date.now() - nodeStart,
    }
  }
}
```

注意：此函数引用了已有的 `VAR_INJECTION_RE`（第 330 行）、`listVariables`、`resolveSqlVariableList`、`getSqlFile`、`getShellFile`、`getDataSource`、`getServer`、`executeShellOnServer`、`executeLocal`、`executeDbQuery`、`resolveVariables`、`readOrchFile`、`QUERY_TIMEOUT_MS` 等变量和函数，均已在文件中定义或导入。

- [ ] **Step 3: 确保类型导入正确**

确认 `orchestration.service.ts` 第 7 行的 import 中已包含 `LoopConfig`（Task 1 中已添加到 types 文件）。如果当前导入是逐个列出类型，需加入 `LoopConfig`。如果导入语句是 `import type { ... } from './orchestration.types.js'`，则确保 `LoopConfig` 在列表中。

当前第 7 行：
```typescript
import type { Orchestration, OrchestrationInput, OrchestrationExecution, LogEntry, OrchestrationNode, OrchestrationEdge, ThresholdOperator, ColumnMapping } from './orchestration.types.js'
```

不需要额外导入，因为 `LoopConfig` 嵌套在 `OrchestrationNode.loop` 字段中，TypeScript 会自动推断。无需修改 import。

- [ ] **Step 4: 提交**

```bash
git add backend/src/modules/orchestration/orchestration.service.ts
git commit -m "feat(loop-node): add executeLoopNode with inline/reference sub-task support"
```

---

### Task 3: 前端 API 类型同步

**Files:**
- Modify: `frontend/src/api/orchestration.ts`

- [ ] **Step 1: 同步后端类型变更**

在前端 `orchestration.ts` 中，镜像后端的类型变更：

1. 在 `OrchestrationNode.type` 中加入 `'loop'`
2. 新增 `LoopSubTaskInline`、`LoopSubTaskReference`、`LoopConfig` 类型
3. 在 `OrchestrationNode` 上新增 `loop?: LoopConfig`
4. 在 `LogEntry.nodeType` 中加入 `'loop'`

修改后的完整类型部分：

```typescript
import { api } from './client'

export type ThresholdOperator = 'eq' | 'ne' | 'gt' | 'gte' | 'lt' | 'lte'

export type ColumnMapping = { source: string; target: string; isPartition?: boolean }

export type LoopSubTaskInline = {
  mode: 'inline'
  type: 'sql' | 'debug' | 'load' | 'wait' | 'shell'
}

export type LoopSubTaskReference = {
  mode: 'reference'
  refType: 'orchestration' | 'sqlFile' | 'shellFile'
  refId: string
  variableName?: string
}

export type LoopConfig = {
  variableId: string
  failureStrategy: 'stop' | 'continue'
  subTask: LoopSubTaskInline | LoopSubTaskReference
}

export type OrchestrationNode = {
  id: string
  name: string
  type: 'sql' | 'debug' | 'load' | 'wait' | 'shell' | 'loop'
  datasourceId: string
  sql: string
  sqlFileId?: string
  enabled: boolean
  thresholdOperator?: ThresholdOperator
  thresholdValue?: string
  targetDatasourceId?: string
  targetTable?: string
  mode?: 'insert' | 'replace' | 'truncate_insert'
  columnMappings?: ColumnMapping[]
  partitionColumns?: string
  loopVariableId?: string
  loop?: LoopConfig
  waitIntervalSec?: number
  waitTimeoutSec?: number
  waitStableCount?: number
  serverId?: string
  shellContent?: string
  shellFileId?: string
  position?: { x: number; y: number }
}

export type OrchestrationEdge = {
  id: string
  source: string
  target: string
}

export type Orchestration = {
  id: string
  name: string
  description: string
  nodes: OrchestrationNode[]
  edges: OrchestrationEdge[]
  createdAt: number
  updatedAt: number
}

export type LogEntry = {
  nodeId: string
  nodeName: string
  nodeType: 'sql' | 'debug' | 'load' | 'wait' | 'shell' | 'loop'
  status: 'success' | 'failed' | 'warning' | 'skipped' | 'running'
  sql: string
  affectedRows?: number
  result?: { columns: string[]; rows: Record<string, unknown>[] }
  thresholdPassed?: boolean
  actualValue?: string
  expectedValue?: string
  error?: string
  timestamp: number
  duration: number
}
```

`OrchestrationApi` 对象保持不变，因为 CRUD 和执行 API 接口没有变化（loop 配置通过 node 数据透传）。

- [ ] **Step 2: 提交**

```bash
git add frontend/src/api/orchestration.ts
git commit -m "feat(loop-node): sync frontend types with backend LoopConfig"
```

---

### Task 4: 前端节点渲染 — LoopNode.tsx

**Files:**
- Create: `frontend/src/components/orchestration/nodes/LoopNode.tsx`

- [ ] **Step 1: 创建 LoopNode 组件**

参考 `ShellNode.tsx` 的模式，创建循环节点渲染组件。显示子任务模式和类型/引用信息。

```typescript
import type { NodeProps, Node } from '@xyflow/react'
import type { OrchestrationNode } from '../../../api/orchestration'
import { BaseNode } from './BaseNode'

type LoopNodeData = Node<OrchestrationNode, 'loop'>

const INLINE_LABEL: Record<string, string> = { sql: 'SQL', debug: 'Debug', load: '加载', wait: 'Wait', shell: 'Shell' }
const REF_LABEL: Record<string, string> = { orchestration: '编排', sqlFile: 'SQL文件', shellFile: 'Shell脚本' }

export function LoopNode({ data, selected }: NodeProps<LoopNodeData>) {
  const loop = data.loop
  let subTaskDesc = '未配置'
  if (loop?.subTask.mode === 'inline') {
    subTaskDesc = INLINE_LABEL[loop.subTask.type] ?? loop.subTask.type
  } else if (loop?.subTask.mode === 'reference') {
    subTaskDesc = REF_LABEL[loop.subTask.refType] ?? loop.subTask.refType
  }
  return (
    <BaseNode data={data} selected={!!selected}>
      <div style={{ color: 'var(--fg-3)', fontSize: 9, marginBottom: 2 }}>
        {loop ? `子任务: ${subTaskDesc}` : '未配置子任务'}
      </div>
      {loop?.variableId && (
        <div style={{ color: 'var(--fg-4)', fontSize: 8, overflow: 'hidden', textOverflow: 'ellipsis', whiteSpace: 'nowrap', maxWidth: 170 }}>
          循环变量: {loop.variableId}
        </div>
      )}
    </BaseNode>
  )
}
```

- [ ] **Step 2: 注册到 FlowEditor**

修改 `frontend/src/components/orchestration/FlowEditor.tsx`：

在 import 区域加入：
```typescript
import { LoopNode } from './nodes/LoopNode'
```

在 `nodeTypes` 对象中加入：
```typescript
const nodeTypes = {
  sql: SqlNode,
  debug: DebugNode,
  load: LoadNode,
  wait: WaitNode,
  shell: ShellNode,
  loop: LoopNode,
}
```

- [ ] **Step 3: 提交**

```bash
git add frontend/src/components/orchestration/nodes/LoopNode.tsx frontend/src/components/orchestration/FlowEditor.tsx
git commit -m "feat(loop-node): add LoopNode component and register in FlowEditor"
```

---

### Task 5: 前端配置表单 — LoopNodeConfig.tsx

**Files:**
- Create: `frontend/src/components/orchestration/panels/LoopNodeConfig.tsx`

- [ ] **Step 1: 创建 LoopNodeConfig 组件**

这是最复杂的前端文件。组件分三部分：循环配置（变量 + 失败策略）、模式切换、子任务配置（内嵌时复用现有 Config 组件，引用时显示资源下拉）。

需要额外从 `OrchestrationApi` 获取编排列表用于引用编排的下拉选择。

```typescript
import { useState, useEffect } from 'react'
import type { OrchestrationNode, LoopConfig, LoopSubTaskInline, LoopSubTaskReference } from '../../../api/orchestration'
import type { DataSource, SqlFile, SqlVariable, Server, ShellFile } from '../../../api/sql'
import type { Orchestration } from '../../../api/orchestration'
import { OrchestrationApi } from '../../../api/orchestration'
import { FormField } from './FormField'
import { SqlNodeConfig } from './SqlNodeConfig'
import { DebugNodeConfig } from './DebugNodeConfig'
import { LoadNodeConfig } from './LoadNodeConfig'
import { WaitNodeConfig } from './WaitNodeConfig'
import { ShellNodeConfig } from './ShellNodeConfig'

export function LoopNodeConfig({
  node, datasources, sqlFiles, servers, shellFiles, variables, onChange,
}: {
  node: OrchestrationNode
  datasources: DataSource[]
  sqlFiles: SqlFile[]
  servers: Server[]
  shellFiles: ShellFile[]
  variables: SqlVariable[]
  onChange: (updates: Partial<OrchestrationNode>) => void
}) {
  const [orchestrations, setOrchestrations] = useState<Orchestration[]>([])
  useEffect(() => { OrchestrationApi.list().then(setOrchestrations).catch(() => {}) }, [])

  const loop = node.loop
  const listVars = variables.filter(v => v.valueType === 'sql' || v.isList)
  const loopVarName = loop?.variableId ? (variables.find(v => v.id === loop.variableId)?.name ?? '') : ''

  const updateLoop = (updates: Partial<LoopConfig>) => {
    onChange({ loop: { ...{ variableId: '', failureStrategy: 'stop', subTask: { mode: 'inline', type: 'sql' } }, ...loop, ...updates } as LoopConfig })
  }

  const updateSubTask = (updates: Partial<LoopSubTaskInline | LoopSubTaskReference>) => {
    const current = loop?.subTask ?? { mode: 'inline', type: 'sql' } as LoopSubTaskInline
    updateLoop({ subTask: { ...current, ...updates } })
  }

  const inlineConfigProps = { node, datasources, sqlFiles, onChange }
  const subTask = loop?.subTask ?? { mode: 'inline', type: 'sql' } as LoopSubTaskInline

  return (
    <>
      <FormField label="节点名称">
        <input className="orch-drawer-input" value={node.name} onChange={(e) => onChange({ name: e.target.value })} />
      </FormField>

      <FormField label="循环变量">
        <select className="orch-drawer-select" value={loop?.variableId ?? ''} onChange={(e) => updateLoop({ variableId: e.target.value })}>
          <option value="">选择列表变量</option>
          {listVars.map(v => (
            <option key={v.id} value={v.id}>{v.name} ({v.valueType}{v.isList ? ' / 列表' : ''})</option>
          ))}
        </select>
        <div style={{ color: 'var(--fg-4)', fontSize: 9, marginTop: 2 }}>
          选择返回列表的 SQL 变量，循环节点会按每个值循环执行子任务
        </div>
      </FormField>

      <FormField label="失败策略">
        <div style={{ display: 'flex', gap: 4 }}>
          {(['stop', 'continue'] as const).map(s => (
            <button key={s} className={`chip small ${(loop?.failureStrategy ?? 'stop') === s ? '' : 'inactive'}`}
              onClick={() => updateLoop({ failureStrategy: s })}>
              {s === 'stop' ? '停止' : '继续'}
            </button>
          ))}
        </div>
      </FormField>

      <FormField label="子任务模式">
        <div style={{ display: 'flex', gap: 4 }}>
          {(['inline', 'reference'] as const).map(m => (
            <button key={m} className={`chip small ${subTask.mode === m ? '' : 'inactive'}`}
              onClick={() => {
                if (m === 'inline') updateSubTask({ mode: 'inline', type: 'sql' })
                else updateSubTask({ mode: 'reference', refType: 'orchestration', refId: '', variableName: '' })
              }}>
              {m === 'inline' ? '内嵌' : '引用'}
            </button>
          ))}
        </div>
      </FormField>

      {subTask.mode === 'inline' ? (
        <>
          <FormField label="子任务类型">
            <select className="orch-drawer-select" value={subTask.type} onChange={(e) => updateSubTask({ type: e.target.value as LoopSubTaskInline['type'] })}>
              <option value="sql">SQL</option>
              <option value="debug">Debug</option>
              <option value="load">加载</option>
              <option value="wait">Wait</option>
              <option value="shell">Shell</option>
            </select>
          </FormField>
          <div style={{ borderTop: '1px solid var(--hairline)', margin: '6px 0' }} />
          {subTask.type === 'sql' && <SqlNodeConfig {...inlineConfigProps} variables={variables} />}
          {subTask.type === 'debug' && <DebugNodeConfig {...inlineConfigProps} />}
          {subTask.type === 'load' && <LoadNodeConfig node={node} datasources={datasources} variables={variables} onChange={onChange} />}
          {subTask.type === 'wait' && <WaitNodeConfig {...inlineConfigProps} />}
          {subTask.type === 'shell' && <ShellNodeConfig node={node} servers={servers} shellFiles={shellFiles} onChange={onChange} />}
        </>
      ) : (
        <>
          <FormField label="引用类型">
            <select className="orch-drawer-select" value={(subTask as LoopSubTaskReference).refType} onChange={(e) => updateSubTask({ refType: e.target.value as LoopSubTaskReference['refType'], refId: '' })}>
              <option value="orchestration">编排</option>
              <option value="sqlFile">SQL 文件</option>
              <option value="shellFile">Shell 脚本</option>
            </select>
          </FormField>

          {(subTask as LoopSubTaskReference).refType === 'orchestration' && (
            <>
              <FormField label="选择编排">
                <select className="orch-drawer-select" value={(subTask as LoopSubTaskReference).refId} onChange={(e) => updateSubTask({ refId: e.target.value })}>
                  <option value="">选择编排</option>
                  {orchestrations.map(o => (
                    <option key={o.id} value={o.id}>{o.name}</option>
                  ))}
                </select>
              </FormField>
              <FormField label="传递变量名">
                <input className="orch-drawer-input" placeholder="子编排中的变量名" value={(subTask as LoopSubTaskReference).variableName ?? ''} onChange={(e) => updateSubTask({ variableName: e.target.value })} />
                <div style={{ color: 'var(--fg-4)', fontSize: 9, marginTop: 2 }}>
                  循环变量的值会注入到子编排中此变量名的位置
                </div>
              </FormField>
            </>
          )}

          {(subTask as LoopSubTaskReference).refType === 'sqlFile' && (
            <>
              <FormField label="选择 SQL 文件">
                <select className="orch-drawer-select" value={(subTask as LoopSubTaskReference).refId} onChange={(e) => updateSubTask({ refId: e.target.value })}>
                  <option value="">选择文件</option>
                  {sqlFiles.map(f => (
                    <option key={f.id} value={f.id}>{f.name}</option>
                  ))}
                </select>
              </FormField>
              <FormField label="数据源（执行用）">
                <select className="orch-drawer-select" value={node.datasourceId} onChange={(e) => onChange({ datasourceId: e.target.value })}>
                  <option value="">选择数据源</option>
                  {datasources.map(ds => (
                    <option key={ds.id} value={ds.id}>{ds.name} ({ds.type})</option>
                  ))}
                </select>
              </FormField>
            </>
          )}

          {(subTask as LoopSubTaskReference).refType === 'shellFile' && (
            <>
              <FormField label="选择 Shell 脚本">
                <select className="orch-drawer-select" value={(subTask as LoopSubTaskReference).refId} onChange={(e) => updateSubTask({ refId: e.target.value })}>
                  <option value="">选择脚本</option>
                  {shellFiles.map(f => (
                    <option key={f.id} value={f.id}>{f.name}</option>
                  ))}
                </select>
              </FormField>
              <FormField label="目标服务器">
                <select className="orch-drawer-select" value={node.serverId ?? ''} onChange={(e) => onChange({ serverId: e.target.value })}>
                  <option value="">本地执行</option>
                  {servers.map(s => (
                    <option key={s.id} value={s.id}>{s.name} ({s.host})</option>
                  ))}
                </select>
              </FormField>
            </>
          )}

          <div style={{ color: 'var(--fg-4)', fontSize: 9, marginTop: 4 }}>
            循环时变量 ${`{`}{loopVarName || '变量名'}${`}`} 会替换为当前值
          </div>
        </>
      )}
    </>
  )
}
```

- [ ] **Step 2: 提交**

```bash
git add frontend/src/components/orchestration/panels/LoopNodeConfig.tsx
git commit -m "feat(loop-node): add LoopNodeConfig with inline/reference sub-task forms"
```

---

### Task 6: 前端集成 — 注册到现有组件

**Files:**
- Modify: `frontend/src/components/orchestration/AddNodeDialog.tsx`
- Modify: `frontend/src/components/orchestration/nodes/BaseNode.tsx`
- Modify: `frontend/src/components/orchestration/panels/NodeConfigDrawer.tsx`
- Modify: `frontend/src/components/orchestration/hooks/useOrchestrationEditor.ts`
- Modify: `frontend/src/pages/SqlOrchestration.tsx`

- [ ] **Step 1: AddNodeDialog.tsx — 加入 loop 选项**

在 `NODE_OPTIONS` 数组末尾添加：

```typescript
  { type: 'loop', label: '循环节点', desc: '列表变量驱动循环执行', iconBg: '#8b5cf6' },
```

- [ ] **Step 2: BaseNode.tsx — 加入 loop 样式**

在 `TYPE_CONFIG` 对象中加入：

```typescript
  loop:  { label: '循环', icon: '⟳', bg: 'var(--node-load-bg)', border: 'var(--node-load-border)', iconBg: '#8b5cf6' },
```

- [ ] **Step 3: NodeConfigDrawer.tsx — 加入 loop 路由**

1. 在 import 区域加入：
```typescript
import { LoopNodeConfig } from './LoopNodeConfig'
```

2. 在 `TYPE_CONFIG` 对象中加入：
```typescript
  loop:  { label: '循环', icon: '⟳', iconBg: '#8b5cf6' },
```

3. 在 `renderForm` 函数的 switch 中，在 `default` 之前加入：
```typescript
      case 'loop':  return <LoopNodeConfig node={node} datasources={datasources} sqlFiles={sqlFiles} servers={servers} shellFiles={shellFiles} variables={variables} onChange={onChange} />
```

4. 更新 `NodeConfigDrawer` 的 props 类型，确保 `servers` 和 `shellFiles` 已传入（当前 `renderForm` 中的 `props` 只解构了 `{ node, datasources, sqlFiles, onChange }`）。需要修改 `renderForm` 来让 `case 'loop'` 能访问到全部 props。

当前 `renderForm` 代码：
```typescript
  const renderForm = () => {
    const props = { node, datasources, sqlFiles, onChange }
    switch (node.type) {
      case 'debug': return <DebugNodeConfig {...props} />
      case 'load':  return <LoadNodeConfig {...props} datasources={datasources} variables={variables} />
      case 'wait':  return <WaitNodeConfig {...props} />
      case 'shell': return <ShellNodeConfig node={node} servers={servers} shellFiles={shellFiles} onChange={onChange} />
      default:      return <SqlNodeConfig {...props} variables={variables} />
    }
  }
```

修改后：
```typescript
  const renderForm = () => {
    const props = { node, datasources, sqlFiles, onChange }
    switch (node.type) {
      case 'debug': return <DebugNodeConfig {...props} />
      case 'load':  return <LoadNodeConfig {...props} datasources={datasources} variables={variables} />
      case 'wait':  return <WaitNodeConfig {...props} />
      case 'shell': return <ShellNodeConfig node={node} servers={servers} shellFiles={shellFiles} onChange={onChange} />
      case 'loop':  return <LoopNodeConfig node={node} datasources={datasources} sqlFiles={sqlFiles} servers={servers} shellFiles={shellFiles} variables={variables} onChange={onChange} />
      default:      return <SqlNodeConfig {...props} variables={variables} />
    }
  }
```

- [ ] **Step 4: useOrchestrationEditor.ts — 加入 loop 类型**

1. 更新 `OrchNodeType`：
```typescript
export type OrchNodeType = 'sql' | 'debug' | 'load' | 'wait' | 'shell' | 'loop'
```

2. 在 `addNode` 函数的 `nameMap` 中加入：
```typescript
const nameMap: Record<OrchNodeType, string> = { sql: 'SQL 节点', debug: 'Debug 节点', load: '加载节点', wait: 'Wait 节点', shell: 'Shell 节点', loop: '循环节点' }
```

3. 在 `addNode` 函数中，在 `...(type === 'shell' ...)` 之后加入 loop 的默认值：
```typescript
      ...(type === 'loop' ? { loop: { variableId: '', failureStrategy: 'stop' as const, subTask: { mode: 'inline' as const, type: 'sql' as const } } } : {}),
```

- [ ] **Step 5: SqlOrchestration.tsx — 传递 orchestration list 到 drawer**

当前 `NodeConfigDrawer` 的调用中已传入 `variables`、`servers`、`shellFiles`，无需额外修改。`LoopNodeConfig` 内部自行通过 `OrchestrationApi.list()` 获取编排列表。

- [ ] **Step 6: 提交**

```bash
git add frontend/src/components/orchestration/AddNodeDialog.tsx frontend/src/components/orchestration/nodes/BaseNode.tsx frontend/src/components/orchestration/panels/NodeConfigDrawer.tsx frontend/src/components/orchestration/hooks/useOrchestrationEditor.ts
git commit -m "feat(loop-node): register loop node in AddNode, BaseNode, Drawer, and editor hook"
```

---

## Self-Review

**1. Spec coverage:**
- 新增 `loop` 节点类型 → Task 1 (types), Task 3 (frontend types), Task 4 (render), Task 6 (registration)
- 内嵌配置子任务 → Task 2 (executeLoopNode inline), Task 5 (LoopNodeConfig inline form)
- 引用已有资源 → Task 2 (executeLoopNode reference), Task 5 (LoopNodeConfig reference form)
- 循环变量引用 SQL 变量 → Task 2 (resolveSqlVariableList), Task 5 (变量下拉)
- 可配置失败策略 → Task 2 (failureStrategy), Task 5 (切换按钮)
- 编排引用传递变量名 → Task 2 (variableName), Task 5 (输入框)
- SQL/Shell 引用用 `${varName}` 替换 → Task 2 (replaceAll)
- 标准卡片外观 → Task 4 (LoopNode + BaseNode TYPE_CONFIG)

**2. Placeholder scan:** 无 TBD/TODO/模糊描述。

**3. Type consistency:** `LoopConfig`、`LoopSubTaskInline`、`LoopSubTaskReference` 在后端 types (Task 1) 和前端 types (Task 3) 中定义一致。`executeLoopNode` 函数签名与 `executeNode` 的调用约定一致。`LoopNodeConfig` 使用的 props 与 `NodeConfigDrawer` 传递的 props 匹配。
